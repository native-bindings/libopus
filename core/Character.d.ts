export default class Character {
    static isTab(ch: number): boolean;
    static isLineBreak(ch: number): boolean;
    static isIdentifierPart(ch: number): boolean;
    static isWhiteSpace(ch: number): boolean;
    static isStringLiteralStart(ch: number): boolean;
    static isIntegerPart(ch: number): boolean;
    static isHexadecimalIntegerPart(ch: number): boolean;
    static isIdentifierStart(ch: number): boolean;
}
//# sourceMappingURL=Character.d.ts.map